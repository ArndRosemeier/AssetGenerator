# Monster pack viewer

Superseded by the library viewer. Use that for humans, monsters, and generated
assets in one window:

```bat
viewer\lab\Lab.bat
```

```bash
cargo run -p lab
```

This crate still opens the Quaternius catalog alone (`cargo run -p monster_pack`).
